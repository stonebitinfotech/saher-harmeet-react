# Copy this file to .env.local and set your own values, then restart `npm run dev`
# (or rebuild for production). These are baked into the build, so treat the
# dashboard as light gating, not real security.
VITE_DASHBOARD_USER=admin
VITE_DASHBOARD_PASSWORD=change-this-password
